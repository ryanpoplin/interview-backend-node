'use strict';

// Buffer is a global object in Node
// const buf = new Buffer('Byrdann Fox', 'utf8');
// buf.toString();
// buf.toJSON();
// buf[2];
// buf.write('wo');

// // es6 typed arrays
// // an easy way to deal with binary data via JavaScript
// // byte: 8 bits
// const buffer = new ArrayBuffer(8); // 64 0's and 1's V8 JS feature
// const view = new Int32Array(buffer); //
// view[0] = 20;
// view[1] = 5;

// module.exports = view;